<resources>

    <!-- Base application theme. -->
    <style name="QiShuiAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--导航栏颜色-->
        <item name="colorPrimary">@color/colorMain</item>
        <!--状态栏颜色 5.0 之后有效-->
        <item name="colorPrimaryDark">@color/colorMain</item>
        <!--控件选中后颜色-->
        <item name="colorAccent">@color/colorMain</item>
    </style>

    <!--防止白屏-->
    <style name="SplashTheme" parent="QiShuiAppTheme">
        <item name="android:windowBackground">@color/colorMain</item>
    </style>

    <!--主页rg样式-->
    <style name="RadioGroupButtonStyle" >
        <!-- 这个属性是去掉button 默认样式-->
        <item name="android:button">@null</item>
        <item name="android:background">@color/colorTransparent</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@drawable/color_selector</item>
    </style>


    <style name="TransparentBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/TransparentBottomSheetStyle</item>
    </style>

    <style name="TransparentBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!--从低到顶动画效果-->
    <style name="dialogWindowAnimButtomToTop" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>


    <style name="dialog_animation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>



</resources>
